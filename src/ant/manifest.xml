<?xml version="1.0" encoding="ISO-8859-1"?>
<project>

    <!-- MANIFEST.MF properties -->
	<property name="implementation.title" value="${ant.project.name}" />
	<property name="implementation.version" value="${project.version}" />
	<property name="implementation.url" value="${project.url}" />

</project>